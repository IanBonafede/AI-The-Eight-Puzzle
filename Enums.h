#ifndef ENUMS_H
#define ENUMS_H

#include <vector>
#include <cmath>
#include <iostream>

using namespace std;

enum Moves {NONE, UP, DOWN, LEFT, RIGHT};
enum Algorithms {UNIFORM_COST_SEARCH, A_STAR_MISPLACED_TILE, A_STAR_MANHATTAN_DISTANCE};


#endif /* ENUMS_H */